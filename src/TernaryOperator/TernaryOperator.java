package TernaryOperator;

public class TernaryOperator {

	public static void main(String[] args) {
		int income = 120_000;
		// Since its not a boolean variable, ternary operator is useful to validate an expression and the 
		// assign a valid value to the variable 
		String className = income > 100_000 ? "First" : "Economic";

	}

}
